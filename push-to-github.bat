@echo off
git config --global user.email "kdesigns236@github.com"
git config --global user.name "kdesigns236"
git commit -m "Add Socket.io WebSocket support"
git branch -M main
git push -u origin main
pause
